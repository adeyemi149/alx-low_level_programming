#include "main.h"

/**
 * _islower - function checks lowercase character
 * @c: parameter character to check
 * Return: integer 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
